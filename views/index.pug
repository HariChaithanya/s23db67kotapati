extends layout

block content
  h1= title
  p Welcome to my home page and enjoy
  button(type="button", class="btn btn-secondary",onclick="goFaster()") Press Me 
  div#faster Increase the Speed.
  
  p Click me to get Results:
    a(href='/guitar') Search Results
  script.
    var mph = 20;
    var change = 1;

    function goFaster() {
      console.log("hi");
      mph += change;
      change += 1;
      document.getElementById('faster').innerText = `MPH ${mph} with change ${change}`;
    }